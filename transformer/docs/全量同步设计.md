## 需求

把数据库中SocialEntity表中的数据全量同步到es和OSS对象存储

## 设计

同步操作通过命令触发 or 通过请求触发？

单个Reader，两个独立的writer服务

效率：

任务分配代码设计

游标分页+多线程同步+任务对象设计

任务执行代码设计

任务表设计

进度记录表设计
进度记录，断点续传

正确性：

应用崩溃处理

远端崩溃超时处理

幂等插入
毒丸数据处理

es表的设计

## 实现

